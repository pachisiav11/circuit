# Copy this file to a file named exactly  .env  (same folder) if you want to override defaults.
# No API keys are needed — CIRCUIT only serves the game and relays online play.

# Optional: change the server port (default 8787)
# PORT=8787
